<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>oneBils!</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        form {
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            /* width: 40%; */
            height: 40% !important;
        }
        h1 {
            font-size: 24px;
            color: #333;
        }
        input[type="file"] {
            display: block;
            margin: 20px auto;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        button[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            cursor: pointer;
        }
        button[type="submit"]:hover {
            background-color: #0056b3;
        }
        .generate-bill-btn{
            background-color: orange;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            border: none;
            font-size: 15px;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <div style="display: flex;justify-content: center;align-items: center;flex-direction: column;">
    <form method="post" action="/generatebill" enctype="multipart/form-data">
        <h1>Upload Customer Bill</h1>
        <input name="userBill" type="file" accept=".csv" required />
        <button type="submit">Generate Bill</button>
    </form>
    <button onclick="location.href = '/profile/653a5b17ef3c567375d8d427'" class="generate-bill-btn">Profile</button>
</div>
</body>
</html>
